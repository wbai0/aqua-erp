{
  "name": "cide-erp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run serve -w server",
    "start:prod": "npm install && npm run cloud:generate -w server && npm run build -w web && npm run serve -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
